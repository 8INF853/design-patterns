import Collection from "./collection"

export default class MovieList {
  constructor() {
    this.movies = new Collection()
  }

  size() {
    this.movies.size()
  }

  add(movie) {
    movies.add(movie)
  }
}